// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"genomedb/ent/keggmodule"
	"genomedb/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// KeggModuleQuery is the builder for querying KeggModule entities.
type KeggModuleQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.KeggModule
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KeggModuleQuery builder.
func (kmq *KeggModuleQuery) Where(ps ...predicate.KeggModule) *KeggModuleQuery {
	kmq.predicates = append(kmq.predicates, ps...)
	return kmq
}

// Limit adds a limit step to the query.
func (kmq *KeggModuleQuery) Limit(limit int) *KeggModuleQuery {
	kmq.limit = &limit
	return kmq
}

// Offset adds an offset step to the query.
func (kmq *KeggModuleQuery) Offset(offset int) *KeggModuleQuery {
	kmq.offset = &offset
	return kmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (kmq *KeggModuleQuery) Unique(unique bool) *KeggModuleQuery {
	kmq.unique = &unique
	return kmq
}

// Order adds an order step to the query.
func (kmq *KeggModuleQuery) Order(o ...OrderFunc) *KeggModuleQuery {
	kmq.order = append(kmq.order, o...)
	return kmq
}

// First returns the first KeggModule entity from the query.
// Returns a *NotFoundError when no KeggModule was found.
func (kmq *KeggModuleQuery) First(ctx context.Context) (*KeggModule, error) {
	nodes, err := kmq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{keggmodule.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (kmq *KeggModuleQuery) FirstX(ctx context.Context) *KeggModule {
	node, err := kmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KeggModule ID from the query.
// Returns a *NotFoundError when no KeggModule ID was found.
func (kmq *KeggModuleQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = kmq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{keggmodule.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (kmq *KeggModuleQuery) FirstIDX(ctx context.Context) string {
	id, err := kmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KeggModule entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KeggModule entity is found.
// Returns a *NotFoundError when no KeggModule entities are found.
func (kmq *KeggModuleQuery) Only(ctx context.Context) (*KeggModule, error) {
	nodes, err := kmq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{keggmodule.Label}
	default:
		return nil, &NotSingularError{keggmodule.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (kmq *KeggModuleQuery) OnlyX(ctx context.Context) *KeggModule {
	node, err := kmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KeggModule ID in the query.
// Returns a *NotSingularError when more than one KeggModule ID is found.
// Returns a *NotFoundError when no entities are found.
func (kmq *KeggModuleQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = kmq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{keggmodule.Label}
	default:
		err = &NotSingularError{keggmodule.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (kmq *KeggModuleQuery) OnlyIDX(ctx context.Context) string {
	id, err := kmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KeggModules.
func (kmq *KeggModuleQuery) All(ctx context.Context) ([]*KeggModule, error) {
	if err := kmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return kmq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (kmq *KeggModuleQuery) AllX(ctx context.Context) []*KeggModule {
	nodes, err := kmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KeggModule IDs.
func (kmq *KeggModuleQuery) IDs(ctx context.Context) ([]string, error) {
	var ids []string
	if err := kmq.Select(keggmodule.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (kmq *KeggModuleQuery) IDsX(ctx context.Context) []string {
	ids, err := kmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (kmq *KeggModuleQuery) Count(ctx context.Context) (int, error) {
	if err := kmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return kmq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (kmq *KeggModuleQuery) CountX(ctx context.Context) int {
	count, err := kmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (kmq *KeggModuleQuery) Exist(ctx context.Context) (bool, error) {
	if err := kmq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return kmq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (kmq *KeggModuleQuery) ExistX(ctx context.Context) bool {
	exist, err := kmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KeggModuleQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (kmq *KeggModuleQuery) Clone() *KeggModuleQuery {
	if kmq == nil {
		return nil
	}
	return &KeggModuleQuery{
		config:     kmq.config,
		limit:      kmq.limit,
		offset:     kmq.offset,
		order:      append([]OrderFunc{}, kmq.order...),
		predicates: append([]predicate.KeggModule{}, kmq.predicates...),
		// clone intermediate query.
		sql:    kmq.sql.Clone(),
		path:   kmq.path,
		unique: kmq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (kmq *KeggModuleQuery) GroupBy(field string, fields ...string) *KeggModuleGroupBy {
	grbuild := &KeggModuleGroupBy{config: kmq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := kmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return kmq.sqlQuery(ctx), nil
	}
	grbuild.label = keggmodule.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (kmq *KeggModuleQuery) Select(fields ...string) *KeggModuleSelect {
	kmq.fields = append(kmq.fields, fields...)
	selbuild := &KeggModuleSelect{KeggModuleQuery: kmq}
	selbuild.label = keggmodule.Label
	selbuild.flds, selbuild.scan = &kmq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a KeggModuleSelect configured with the given aggregations.
func (kmq *KeggModuleQuery) Aggregate(fns ...AggregateFunc) *KeggModuleSelect {
	return kmq.Select().Aggregate(fns...)
}

func (kmq *KeggModuleQuery) prepareQuery(ctx context.Context) error {
	for _, f := range kmq.fields {
		if !keggmodule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if kmq.path != nil {
		prev, err := kmq.path(ctx)
		if err != nil {
			return err
		}
		kmq.sql = prev
	}
	return nil
}

func (kmq *KeggModuleQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KeggModule, error) {
	var (
		nodes = []*KeggModule{}
		_spec = kmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KeggModule).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KeggModule{config: kmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, kmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (kmq *KeggModuleQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := kmq.querySpec()
	_spec.Node.Columns = kmq.fields
	if len(kmq.fields) > 0 {
		_spec.Unique = kmq.unique != nil && *kmq.unique
	}
	return sqlgraph.CountNodes(ctx, kmq.driver, _spec)
}

func (kmq *KeggModuleQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := kmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (kmq *KeggModuleQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   keggmodule.Table,
			Columns: keggmodule.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: keggmodule.FieldID,
			},
		},
		From:   kmq.sql,
		Unique: true,
	}
	if unique := kmq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := kmq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, keggmodule.FieldID)
		for i := range fields {
			if fields[i] != keggmodule.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := kmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := kmq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := kmq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := kmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (kmq *KeggModuleQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(kmq.driver.Dialect())
	t1 := builder.Table(keggmodule.Table)
	columns := kmq.fields
	if len(columns) == 0 {
		columns = keggmodule.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if kmq.sql != nil {
		selector = kmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if kmq.unique != nil && *kmq.unique {
		selector.Distinct()
	}
	for _, p := range kmq.predicates {
		p(selector)
	}
	for _, p := range kmq.order {
		p(selector)
	}
	if offset := kmq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := kmq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// KeggModuleGroupBy is the group-by builder for KeggModule entities.
type KeggModuleGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (kmgb *KeggModuleGroupBy) Aggregate(fns ...AggregateFunc) *KeggModuleGroupBy {
	kmgb.fns = append(kmgb.fns, fns...)
	return kmgb
}

// Scan applies the group-by query and scans the result into the given value.
func (kmgb *KeggModuleGroupBy) Scan(ctx context.Context, v any) error {
	query, err := kmgb.path(ctx)
	if err != nil {
		return err
	}
	kmgb.sql = query
	return kmgb.sqlScan(ctx, v)
}

func (kmgb *KeggModuleGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range kmgb.fields {
		if !keggmodule.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := kmgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kmgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (kmgb *KeggModuleGroupBy) sqlQuery() *sql.Selector {
	selector := kmgb.sql.Select()
	aggregation := make([]string, 0, len(kmgb.fns))
	for _, fn := range kmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(kmgb.fields)+len(kmgb.fns))
		for _, f := range kmgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(kmgb.fields...)...)
}

// KeggModuleSelect is the builder for selecting fields of KeggModule entities.
type KeggModuleSelect struct {
	*KeggModuleQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (kms *KeggModuleSelect) Aggregate(fns ...AggregateFunc) *KeggModuleSelect {
	kms.fns = append(kms.fns, fns...)
	return kms
}

// Scan applies the selector query and scans the result into the given value.
func (kms *KeggModuleSelect) Scan(ctx context.Context, v any) error {
	if err := kms.prepareQuery(ctx); err != nil {
		return err
	}
	kms.sql = kms.KeggModuleQuery.sqlQuery(ctx)
	return kms.sqlScan(ctx, v)
}

func (kms *KeggModuleSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(kms.fns))
	for _, fn := range kms.fns {
		aggregation = append(aggregation, fn(kms.sql))
	}
	switch n := len(*kms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		kms.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		kms.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := kms.sql.Query()
	if err := kms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
