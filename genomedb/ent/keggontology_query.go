// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"genomedb/ent/keggontology"
	"genomedb/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// KeggOntologyQuery is the builder for querying KeggOntology entities.
type KeggOntologyQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.KeggOntology
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the KeggOntologyQuery builder.
func (koq *KeggOntologyQuery) Where(ps ...predicate.KeggOntology) *KeggOntologyQuery {
	koq.predicates = append(koq.predicates, ps...)
	return koq
}

// Limit adds a limit step to the query.
func (koq *KeggOntologyQuery) Limit(limit int) *KeggOntologyQuery {
	koq.limit = &limit
	return koq
}

// Offset adds an offset step to the query.
func (koq *KeggOntologyQuery) Offset(offset int) *KeggOntologyQuery {
	koq.offset = &offset
	return koq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (koq *KeggOntologyQuery) Unique(unique bool) *KeggOntologyQuery {
	koq.unique = &unique
	return koq
}

// Order adds an order step to the query.
func (koq *KeggOntologyQuery) Order(o ...OrderFunc) *KeggOntologyQuery {
	koq.order = append(koq.order, o...)
	return koq
}

// First returns the first KeggOntology entity from the query.
// Returns a *NotFoundError when no KeggOntology was found.
func (koq *KeggOntologyQuery) First(ctx context.Context) (*KeggOntology, error) {
	nodes, err := koq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{keggontology.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (koq *KeggOntologyQuery) FirstX(ctx context.Context) *KeggOntology {
	node, err := koq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first KeggOntology ID from the query.
// Returns a *NotFoundError when no KeggOntology ID was found.
func (koq *KeggOntologyQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = koq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{keggontology.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (koq *KeggOntologyQuery) FirstIDX(ctx context.Context) string {
	id, err := koq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single KeggOntology entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one KeggOntology entity is found.
// Returns a *NotFoundError when no KeggOntology entities are found.
func (koq *KeggOntologyQuery) Only(ctx context.Context) (*KeggOntology, error) {
	nodes, err := koq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{keggontology.Label}
	default:
		return nil, &NotSingularError{keggontology.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (koq *KeggOntologyQuery) OnlyX(ctx context.Context) *KeggOntology {
	node, err := koq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only KeggOntology ID in the query.
// Returns a *NotSingularError when more than one KeggOntology ID is found.
// Returns a *NotFoundError when no entities are found.
func (koq *KeggOntologyQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = koq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{keggontology.Label}
	default:
		err = &NotSingularError{keggontology.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (koq *KeggOntologyQuery) OnlyIDX(ctx context.Context) string {
	id, err := koq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of KeggOntologies.
func (koq *KeggOntologyQuery) All(ctx context.Context) ([]*KeggOntology, error) {
	if err := koq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return koq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (koq *KeggOntologyQuery) AllX(ctx context.Context) []*KeggOntology {
	nodes, err := koq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of KeggOntology IDs.
func (koq *KeggOntologyQuery) IDs(ctx context.Context) ([]string, error) {
	var ids []string
	if err := koq.Select(keggontology.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (koq *KeggOntologyQuery) IDsX(ctx context.Context) []string {
	ids, err := koq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (koq *KeggOntologyQuery) Count(ctx context.Context) (int, error) {
	if err := koq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return koq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (koq *KeggOntologyQuery) CountX(ctx context.Context) int {
	count, err := koq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (koq *KeggOntologyQuery) Exist(ctx context.Context) (bool, error) {
	if err := koq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return koq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (koq *KeggOntologyQuery) ExistX(ctx context.Context) bool {
	exist, err := koq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the KeggOntologyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (koq *KeggOntologyQuery) Clone() *KeggOntologyQuery {
	if koq == nil {
		return nil
	}
	return &KeggOntologyQuery{
		config:     koq.config,
		limit:      koq.limit,
		offset:     koq.offset,
		order:      append([]OrderFunc{}, koq.order...),
		predicates: append([]predicate.KeggOntology{}, koq.predicates...),
		// clone intermediate query.
		sql:    koq.sql.Clone(),
		path:   koq.path,
		unique: koq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.KeggOntology.Query().
//		GroupBy(keggontology.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (koq *KeggOntologyQuery) GroupBy(field string, fields ...string) *KeggOntologyGroupBy {
	grbuild := &KeggOntologyGroupBy{config: koq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := koq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return koq.sqlQuery(ctx), nil
	}
	grbuild.label = keggontology.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.KeggOntology.Query().
//		Select(keggontology.FieldName).
//		Scan(ctx, &v)
func (koq *KeggOntologyQuery) Select(fields ...string) *KeggOntologySelect {
	koq.fields = append(koq.fields, fields...)
	selbuild := &KeggOntologySelect{KeggOntologyQuery: koq}
	selbuild.label = keggontology.Label
	selbuild.flds, selbuild.scan = &koq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a KeggOntologySelect configured with the given aggregations.
func (koq *KeggOntologyQuery) Aggregate(fns ...AggregateFunc) *KeggOntologySelect {
	return koq.Select().Aggregate(fns...)
}

func (koq *KeggOntologyQuery) prepareQuery(ctx context.Context) error {
	for _, f := range koq.fields {
		if !keggontology.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if koq.path != nil {
		prev, err := koq.path(ctx)
		if err != nil {
			return err
		}
		koq.sql = prev
	}
	return nil
}

func (koq *KeggOntologyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*KeggOntology, error) {
	var (
		nodes = []*KeggOntology{}
		_spec = koq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*KeggOntology).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &KeggOntology{config: koq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, koq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (koq *KeggOntologyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := koq.querySpec()
	_spec.Node.Columns = koq.fields
	if len(koq.fields) > 0 {
		_spec.Unique = koq.unique != nil && *koq.unique
	}
	return sqlgraph.CountNodes(ctx, koq.driver, _spec)
}

func (koq *KeggOntologyQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := koq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (koq *KeggOntologyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   keggontology.Table,
			Columns: keggontology.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeString,
				Column: keggontology.FieldID,
			},
		},
		From:   koq.sql,
		Unique: true,
	}
	if unique := koq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := koq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, keggontology.FieldID)
		for i := range fields {
			if fields[i] != keggontology.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := koq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := koq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := koq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := koq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (koq *KeggOntologyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(koq.driver.Dialect())
	t1 := builder.Table(keggontology.Table)
	columns := koq.fields
	if len(columns) == 0 {
		columns = keggontology.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if koq.sql != nil {
		selector = koq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if koq.unique != nil && *koq.unique {
		selector.Distinct()
	}
	for _, p := range koq.predicates {
		p(selector)
	}
	for _, p := range koq.order {
		p(selector)
	}
	if offset := koq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := koq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// KeggOntologyGroupBy is the group-by builder for KeggOntology entities.
type KeggOntologyGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (kogb *KeggOntologyGroupBy) Aggregate(fns ...AggregateFunc) *KeggOntologyGroupBy {
	kogb.fns = append(kogb.fns, fns...)
	return kogb
}

// Scan applies the group-by query and scans the result into the given value.
func (kogb *KeggOntologyGroupBy) Scan(ctx context.Context, v any) error {
	query, err := kogb.path(ctx)
	if err != nil {
		return err
	}
	kogb.sql = query
	return kogb.sqlScan(ctx, v)
}

func (kogb *KeggOntologyGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range kogb.fields {
		if !keggontology.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := kogb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := kogb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (kogb *KeggOntologyGroupBy) sqlQuery() *sql.Selector {
	selector := kogb.sql.Select()
	aggregation := make([]string, 0, len(kogb.fns))
	for _, fn := range kogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(kogb.fields)+len(kogb.fns))
		for _, f := range kogb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(kogb.fields...)...)
}

// KeggOntologySelect is the builder for selecting fields of KeggOntology entities.
type KeggOntologySelect struct {
	*KeggOntologyQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (kos *KeggOntologySelect) Aggregate(fns ...AggregateFunc) *KeggOntologySelect {
	kos.fns = append(kos.fns, fns...)
	return kos
}

// Scan applies the selector query and scans the result into the given value.
func (kos *KeggOntologySelect) Scan(ctx context.Context, v any) error {
	if err := kos.prepareQuery(ctx); err != nil {
		return err
	}
	kos.sql = kos.KeggOntologyQuery.sqlQuery(ctx)
	return kos.sqlScan(ctx, v)
}

func (kos *KeggOntologySelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(kos.fns))
	for _, fn := range kos.fns {
		aggregation = append(aggregation, fn(kos.sql))
	}
	switch n := len(*kos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		kos.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		kos.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := kos.sql.Query()
	if err := kos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
