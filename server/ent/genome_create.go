// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"genomedb/ent/genome"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// GenomeCreate is the builder for creating a Genome entity.
type GenomeCreate struct {
	config
	mutation *GenomeMutation
	hooks    []Hook
	conflict []sql.ConflictOption
}

// SetName sets the "name" field.
func (gc *GenomeCreate) SetName(s string) *GenomeCreate {
	gc.mutation.SetName(s)
	return gc
}

// SetCodonTable sets the "codon_table" field.
func (gc *GenomeCreate) SetCodonTable(i int32) *GenomeCreate {
	gc.mutation.SetCodonTable(i)
	return gc
}

// SetSeq sets the "seq" field.
func (gc *GenomeCreate) SetSeq(s string) *GenomeCreate {
	gc.mutation.SetSeq(s)
	return gc
}

// Mutation returns the GenomeMutation object of the builder.
func (gc *GenomeCreate) Mutation() *GenomeMutation {
	return gc.mutation
}

// Save creates the Genome in the database.
func (gc *GenomeCreate) Save(ctx context.Context) (*Genome, error) {
	var (
		err  error
		node *Genome
	)
	if len(gc.hooks) == 0 {
		if err = gc.check(); err != nil {
			return nil, err
		}
		node, err = gc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*GenomeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = gc.check(); err != nil {
				return nil, err
			}
			gc.mutation = mutation
			if node, err = gc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(gc.hooks) - 1; i >= 0; i-- {
			if gc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = gc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, gc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*Genome)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from GenomeMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (gc *GenomeCreate) SaveX(ctx context.Context) *Genome {
	v, err := gc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (gc *GenomeCreate) Exec(ctx context.Context) error {
	_, err := gc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gc *GenomeCreate) ExecX(ctx context.Context) {
	if err := gc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (gc *GenomeCreate) check() error {
	if _, ok := gc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Genome.name"`)}
	}
	if _, ok := gc.mutation.CodonTable(); !ok {
		return &ValidationError{Name: "codon_table", err: errors.New(`ent: missing required field "Genome.codon_table"`)}
	}
	if _, ok := gc.mutation.Seq(); !ok {
		return &ValidationError{Name: "seq", err: errors.New(`ent: missing required field "Genome.seq"`)}
	}
	return nil
}

func (gc *GenomeCreate) sqlSave(ctx context.Context) (*Genome, error) {
	_node, _spec := gc.createSpec()
	if err := sqlgraph.CreateNode(ctx, gc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (gc *GenomeCreate) createSpec() (*Genome, *sqlgraph.CreateSpec) {
	var (
		_node = &Genome{config: gc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: genome.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: genome.FieldID,
			},
		}
	)
	_spec.OnConflict = gc.conflict
	if value, ok := gc.mutation.Name(); ok {
		_spec.SetField(genome.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := gc.mutation.CodonTable(); ok {
		_spec.SetField(genome.FieldCodonTable, field.TypeInt32, value)
		_node.CodonTable = value
	}
	if value, ok := gc.mutation.Seq(); ok {
		_spec.SetField(genome.FieldSeq, field.TypeString, value)
		_node.Seq = value
	}
	return _node, _spec
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.Genome.Create().
//		SetName(v).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.GenomeUpsert) {
//			SetName(v+v).
//		}).
//		Exec(ctx)
func (gc *GenomeCreate) OnConflict(opts ...sql.ConflictOption) *GenomeUpsertOne {
	gc.conflict = opts
	return &GenomeUpsertOne{
		create: gc,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.Genome.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (gc *GenomeCreate) OnConflictColumns(columns ...string) *GenomeUpsertOne {
	gc.conflict = append(gc.conflict, sql.ConflictColumns(columns...))
	return &GenomeUpsertOne{
		create: gc,
	}
}

type (
	// GenomeUpsertOne is the builder for "upsert"-ing
	//  one Genome node.
	GenomeUpsertOne struct {
		create *GenomeCreate
	}

	// GenomeUpsert is the "OnConflict" setter.
	GenomeUpsert struct {
		*sql.UpdateSet
	}
)

// SetName sets the "name" field.
func (u *GenomeUpsert) SetName(v string) *GenomeUpsert {
	u.Set(genome.FieldName, v)
	return u
}

// UpdateName sets the "name" field to the value that was provided on create.
func (u *GenomeUpsert) UpdateName() *GenomeUpsert {
	u.SetExcluded(genome.FieldName)
	return u
}

// SetCodonTable sets the "codon_table" field.
func (u *GenomeUpsert) SetCodonTable(v int32) *GenomeUpsert {
	u.Set(genome.FieldCodonTable, v)
	return u
}

// UpdateCodonTable sets the "codon_table" field to the value that was provided on create.
func (u *GenomeUpsert) UpdateCodonTable() *GenomeUpsert {
	u.SetExcluded(genome.FieldCodonTable)
	return u
}

// AddCodonTable adds v to the "codon_table" field.
func (u *GenomeUpsert) AddCodonTable(v int32) *GenomeUpsert {
	u.Add(genome.FieldCodonTable, v)
	return u
}

// SetSeq sets the "seq" field.
func (u *GenomeUpsert) SetSeq(v string) *GenomeUpsert {
	u.Set(genome.FieldSeq, v)
	return u
}

// UpdateSeq sets the "seq" field to the value that was provided on create.
func (u *GenomeUpsert) UpdateSeq() *GenomeUpsert {
	u.SetExcluded(genome.FieldSeq)
	return u
}

// UpdateNewValues updates the mutable fields using the new values that were set on create.
// Using this option is equivalent to using:
//
//	client.Genome.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//		).
//		Exec(ctx)
func (u *GenomeUpsertOne) UpdateNewValues() *GenomeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.Genome.Create().
//	    OnConflict(sql.ResolveWithIgnore()).
//	    Exec(ctx)
func (u *GenomeUpsertOne) Ignore() *GenomeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *GenomeUpsertOne) DoNothing() *GenomeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the GenomeCreate.OnConflict
// documentation for more info.
func (u *GenomeUpsertOne) Update(set func(*GenomeUpsert)) *GenomeUpsertOne {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&GenomeUpsert{UpdateSet: update})
	}))
	return u
}

// SetName sets the "name" field.
func (u *GenomeUpsertOne) SetName(v string) *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.SetName(v)
	})
}

// UpdateName sets the "name" field to the value that was provided on create.
func (u *GenomeUpsertOne) UpdateName() *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.UpdateName()
	})
}

// SetCodonTable sets the "codon_table" field.
func (u *GenomeUpsertOne) SetCodonTable(v int32) *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.SetCodonTable(v)
	})
}

// AddCodonTable adds v to the "codon_table" field.
func (u *GenomeUpsertOne) AddCodonTable(v int32) *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.AddCodonTable(v)
	})
}

// UpdateCodonTable sets the "codon_table" field to the value that was provided on create.
func (u *GenomeUpsertOne) UpdateCodonTable() *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.UpdateCodonTable()
	})
}

// SetSeq sets the "seq" field.
func (u *GenomeUpsertOne) SetSeq(v string) *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.SetSeq(v)
	})
}

// UpdateSeq sets the "seq" field to the value that was provided on create.
func (u *GenomeUpsertOne) UpdateSeq() *GenomeUpsertOne {
	return u.Update(func(s *GenomeUpsert) {
		s.UpdateSeq()
	})
}

// Exec executes the query.
func (u *GenomeUpsertOne) Exec(ctx context.Context) error {
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for GenomeCreate.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *GenomeUpsertOne) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}

// Exec executes the UPSERT query and returns the inserted/updated ID.
func (u *GenomeUpsertOne) ID(ctx context.Context) (id int, err error) {
	node, err := u.create.Save(ctx)
	if err != nil {
		return id, err
	}
	return node.ID, nil
}

// IDX is like ID, but panics if an error occurs.
func (u *GenomeUpsertOne) IDX(ctx context.Context) int {
	id, err := u.ID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// GenomeCreateBulk is the builder for creating many Genome entities in bulk.
type GenomeCreateBulk struct {
	config
	builders []*GenomeCreate
	conflict []sql.ConflictOption
}

// Save creates the Genome entities in the database.
func (gcb *GenomeCreateBulk) Save(ctx context.Context) ([]*Genome, error) {
	specs := make([]*sqlgraph.CreateSpec, len(gcb.builders))
	nodes := make([]*Genome, len(gcb.builders))
	mutators := make([]Mutator, len(gcb.builders))
	for i := range gcb.builders {
		func(i int, root context.Context) {
			builder := gcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*GenomeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, gcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					spec.OnConflict = gcb.conflict
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, gcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, gcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (gcb *GenomeCreateBulk) SaveX(ctx context.Context) []*Genome {
	v, err := gcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (gcb *GenomeCreateBulk) Exec(ctx context.Context) error {
	_, err := gcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (gcb *GenomeCreateBulk) ExecX(ctx context.Context) {
	if err := gcb.Exec(ctx); err != nil {
		panic(err)
	}
}

// OnConflict allows configuring the `ON CONFLICT` / `ON DUPLICATE KEY` clause
// of the `INSERT` statement. For example:
//
//	client.Genome.CreateBulk(builders...).
//		OnConflict(
//			// Update the row with the new values
//			// the was proposed for insertion.
//			sql.ResolveWithNewValues(),
//		).
//		// Override some of the fields with custom
//		// update values.
//		Update(func(u *ent.GenomeUpsert) {
//			SetName(v+v).
//		}).
//		Exec(ctx)
func (gcb *GenomeCreateBulk) OnConflict(opts ...sql.ConflictOption) *GenomeUpsertBulk {
	gcb.conflict = opts
	return &GenomeUpsertBulk{
		create: gcb,
	}
}

// OnConflictColumns calls `OnConflict` and configures the columns
// as conflict target. Using this option is equivalent to using:
//
//	client.Genome.Create().
//		OnConflict(sql.ConflictColumns(columns...)).
//		Exec(ctx)
func (gcb *GenomeCreateBulk) OnConflictColumns(columns ...string) *GenomeUpsertBulk {
	gcb.conflict = append(gcb.conflict, sql.ConflictColumns(columns...))
	return &GenomeUpsertBulk{
		create: gcb,
	}
}

// GenomeUpsertBulk is the builder for "upsert"-ing
// a bulk of Genome nodes.
type GenomeUpsertBulk struct {
	create *GenomeCreateBulk
}

// UpdateNewValues updates the mutable fields using the new values that
// were set on create. Using this option is equivalent to using:
//
//	client.Genome.Create().
//		OnConflict(
//			sql.ResolveWithNewValues(),
//		).
//		Exec(ctx)
func (u *GenomeUpsertBulk) UpdateNewValues() *GenomeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithNewValues())
	return u
}

// Ignore sets each column to itself in case of conflict.
// Using this option is equivalent to using:
//
//	client.Genome.Create().
//		OnConflict(sql.ResolveWithIgnore()).
//		Exec(ctx)
func (u *GenomeUpsertBulk) Ignore() *GenomeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWithIgnore())
	return u
}

// DoNothing configures the conflict_action to `DO NOTHING`.
// Supported only by SQLite and PostgreSQL.
func (u *GenomeUpsertBulk) DoNothing() *GenomeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.DoNothing())
	return u
}

// Update allows overriding fields `UPDATE` values. See the GenomeCreateBulk.OnConflict
// documentation for more info.
func (u *GenomeUpsertBulk) Update(set func(*GenomeUpsert)) *GenomeUpsertBulk {
	u.create.conflict = append(u.create.conflict, sql.ResolveWith(func(update *sql.UpdateSet) {
		set(&GenomeUpsert{UpdateSet: update})
	}))
	return u
}

// SetName sets the "name" field.
func (u *GenomeUpsertBulk) SetName(v string) *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.SetName(v)
	})
}

// UpdateName sets the "name" field to the value that was provided on create.
func (u *GenomeUpsertBulk) UpdateName() *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.UpdateName()
	})
}

// SetCodonTable sets the "codon_table" field.
func (u *GenomeUpsertBulk) SetCodonTable(v int32) *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.SetCodonTable(v)
	})
}

// AddCodonTable adds v to the "codon_table" field.
func (u *GenomeUpsertBulk) AddCodonTable(v int32) *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.AddCodonTable(v)
	})
}

// UpdateCodonTable sets the "codon_table" field to the value that was provided on create.
func (u *GenomeUpsertBulk) UpdateCodonTable() *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.UpdateCodonTable()
	})
}

// SetSeq sets the "seq" field.
func (u *GenomeUpsertBulk) SetSeq(v string) *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.SetSeq(v)
	})
}

// UpdateSeq sets the "seq" field to the value that was provided on create.
func (u *GenomeUpsertBulk) UpdateSeq() *GenomeUpsertBulk {
	return u.Update(func(s *GenomeUpsert) {
		s.UpdateSeq()
	})
}

// Exec executes the query.
func (u *GenomeUpsertBulk) Exec(ctx context.Context) error {
	for i, b := range u.create.builders {
		if len(b.conflict) != 0 {
			return fmt.Errorf("ent: OnConflict was set for builder %d. Set it on the GenomeCreateBulk instead", i)
		}
	}
	if len(u.create.conflict) == 0 {
		return errors.New("ent: missing options for GenomeCreateBulk.OnConflict")
	}
	return u.create.Exec(ctx)
}

// ExecX is like Exec, but panics if an error occurs.
func (u *GenomeUpsertBulk) ExecX(ctx context.Context) {
	if err := u.create.Exec(ctx); err != nil {
		panic(err)
	}
}
