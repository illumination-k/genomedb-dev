// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"genomedb/ent/predicate"
	"genomedb/ent/trasnscriptstructure"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TrasnscriptStructureQuery is the builder for querying TrasnscriptStructure entities.
type TrasnscriptStructureQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.TrasnscriptStructure
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TrasnscriptStructureQuery builder.
func (tsq *TrasnscriptStructureQuery) Where(ps ...predicate.TrasnscriptStructure) *TrasnscriptStructureQuery {
	tsq.predicates = append(tsq.predicates, ps...)
	return tsq
}

// Limit adds a limit step to the query.
func (tsq *TrasnscriptStructureQuery) Limit(limit int) *TrasnscriptStructureQuery {
	tsq.limit = &limit
	return tsq
}

// Offset adds an offset step to the query.
func (tsq *TrasnscriptStructureQuery) Offset(offset int) *TrasnscriptStructureQuery {
	tsq.offset = &offset
	return tsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tsq *TrasnscriptStructureQuery) Unique(unique bool) *TrasnscriptStructureQuery {
	tsq.unique = &unique
	return tsq
}

// Order adds an order step to the query.
func (tsq *TrasnscriptStructureQuery) Order(o ...OrderFunc) *TrasnscriptStructureQuery {
	tsq.order = append(tsq.order, o...)
	return tsq
}

// First returns the first TrasnscriptStructure entity from the query.
// Returns a *NotFoundError when no TrasnscriptStructure was found.
func (tsq *TrasnscriptStructureQuery) First(ctx context.Context) (*TrasnscriptStructure, error) {
	nodes, err := tsq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{trasnscriptstructure.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) FirstX(ctx context.Context) *TrasnscriptStructure {
	node, err := tsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TrasnscriptStructure ID from the query.
// Returns a *NotFoundError when no TrasnscriptStructure ID was found.
func (tsq *TrasnscriptStructureQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tsq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{trasnscriptstructure.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) FirstIDX(ctx context.Context) int {
	id, err := tsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TrasnscriptStructure entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TrasnscriptStructure entity is found.
// Returns a *NotFoundError when no TrasnscriptStructure entities are found.
func (tsq *TrasnscriptStructureQuery) Only(ctx context.Context) (*TrasnscriptStructure, error) {
	nodes, err := tsq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{trasnscriptstructure.Label}
	default:
		return nil, &NotSingularError{trasnscriptstructure.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) OnlyX(ctx context.Context) *TrasnscriptStructure {
	node, err := tsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TrasnscriptStructure ID in the query.
// Returns a *NotSingularError when more than one TrasnscriptStructure ID is found.
// Returns a *NotFoundError when no entities are found.
func (tsq *TrasnscriptStructureQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tsq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{trasnscriptstructure.Label}
	default:
		err = &NotSingularError{trasnscriptstructure.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) OnlyIDX(ctx context.Context) int {
	id, err := tsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TrasnscriptStructures.
func (tsq *TrasnscriptStructureQuery) All(ctx context.Context) ([]*TrasnscriptStructure, error) {
	if err := tsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return tsq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) AllX(ctx context.Context) []*TrasnscriptStructure {
	nodes, err := tsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TrasnscriptStructure IDs.
func (tsq *TrasnscriptStructureQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := tsq.Select(trasnscriptstructure.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) IDsX(ctx context.Context) []int {
	ids, err := tsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tsq *TrasnscriptStructureQuery) Count(ctx context.Context) (int, error) {
	if err := tsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return tsq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) CountX(ctx context.Context) int {
	count, err := tsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tsq *TrasnscriptStructureQuery) Exist(ctx context.Context) (bool, error) {
	if err := tsq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return tsq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (tsq *TrasnscriptStructureQuery) ExistX(ctx context.Context) bool {
	exist, err := tsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TrasnscriptStructureQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tsq *TrasnscriptStructureQuery) Clone() *TrasnscriptStructureQuery {
	if tsq == nil {
		return nil
	}
	return &TrasnscriptStructureQuery{
		config:     tsq.config,
		limit:      tsq.limit,
		offset:     tsq.offset,
		order:      append([]OrderFunc{}, tsq.order...),
		predicates: append([]predicate.TrasnscriptStructure{}, tsq.predicates...),
		// clone intermediate query.
		sql:    tsq.sql.Clone(),
		path:   tsq.path,
		unique: tsq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TranscriptId string `json:"transcriptId,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TrasnscriptStructure.Query().
//		GroupBy(trasnscriptstructure.FieldTranscriptId).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tsq *TrasnscriptStructureQuery) GroupBy(field string, fields ...string) *TrasnscriptStructureGroupBy {
	grbuild := &TrasnscriptStructureGroupBy{config: tsq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := tsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return tsq.sqlQuery(ctx), nil
	}
	grbuild.label = trasnscriptstructure.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TranscriptId string `json:"transcriptId,omitempty"`
//	}
//
//	client.TrasnscriptStructure.Query().
//		Select(trasnscriptstructure.FieldTranscriptId).
//		Scan(ctx, &v)
func (tsq *TrasnscriptStructureQuery) Select(fields ...string) *TrasnscriptStructureSelect {
	tsq.fields = append(tsq.fields, fields...)
	selbuild := &TrasnscriptStructureSelect{TrasnscriptStructureQuery: tsq}
	selbuild.label = trasnscriptstructure.Label
	selbuild.flds, selbuild.scan = &tsq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a TrasnscriptStructureSelect configured with the given aggregations.
func (tsq *TrasnscriptStructureQuery) Aggregate(fns ...AggregateFunc) *TrasnscriptStructureSelect {
	return tsq.Select().Aggregate(fns...)
}

func (tsq *TrasnscriptStructureQuery) prepareQuery(ctx context.Context) error {
	for _, f := range tsq.fields {
		if !trasnscriptstructure.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tsq.path != nil {
		prev, err := tsq.path(ctx)
		if err != nil {
			return err
		}
		tsq.sql = prev
	}
	return nil
}

func (tsq *TrasnscriptStructureQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TrasnscriptStructure, error) {
	var (
		nodes = []*TrasnscriptStructure{}
		_spec = tsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TrasnscriptStructure).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TrasnscriptStructure{config: tsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tsq *TrasnscriptStructureQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tsq.querySpec()
	_spec.Node.Columns = tsq.fields
	if len(tsq.fields) > 0 {
		_spec.Unique = tsq.unique != nil && *tsq.unique
	}
	return sqlgraph.CountNodes(ctx, tsq.driver, _spec)
}

func (tsq *TrasnscriptStructureQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := tsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (tsq *TrasnscriptStructureQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   trasnscriptstructure.Table,
			Columns: trasnscriptstructure.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: trasnscriptstructure.FieldID,
			},
		},
		From:   tsq.sql,
		Unique: true,
	}
	if unique := tsq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := tsq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, trasnscriptstructure.FieldID)
		for i := range fields {
			if fields[i] != trasnscriptstructure.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tsq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tsq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tsq *TrasnscriptStructureQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tsq.driver.Dialect())
	t1 := builder.Table(trasnscriptstructure.Table)
	columns := tsq.fields
	if len(columns) == 0 {
		columns = trasnscriptstructure.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tsq.sql != nil {
		selector = tsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tsq.unique != nil && *tsq.unique {
		selector.Distinct()
	}
	for _, p := range tsq.predicates {
		p(selector)
	}
	for _, p := range tsq.order {
		p(selector)
	}
	if offset := tsq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tsq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TrasnscriptStructureGroupBy is the group-by builder for TrasnscriptStructure entities.
type TrasnscriptStructureGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tsgb *TrasnscriptStructureGroupBy) Aggregate(fns ...AggregateFunc) *TrasnscriptStructureGroupBy {
	tsgb.fns = append(tsgb.fns, fns...)
	return tsgb
}

// Scan applies the group-by query and scans the result into the given value.
func (tsgb *TrasnscriptStructureGroupBy) Scan(ctx context.Context, v any) error {
	query, err := tsgb.path(ctx)
	if err != nil {
		return err
	}
	tsgb.sql = query
	return tsgb.sqlScan(ctx, v)
}

func (tsgb *TrasnscriptStructureGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range tsgb.fields {
		if !trasnscriptstructure.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := tsgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tsgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (tsgb *TrasnscriptStructureGroupBy) sqlQuery() *sql.Selector {
	selector := tsgb.sql.Select()
	aggregation := make([]string, 0, len(tsgb.fns))
	for _, fn := range tsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(tsgb.fields)+len(tsgb.fns))
		for _, f := range tsgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(tsgb.fields...)...)
}

// TrasnscriptStructureSelect is the builder for selecting fields of TrasnscriptStructure entities.
type TrasnscriptStructureSelect struct {
	*TrasnscriptStructureQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tss *TrasnscriptStructureSelect) Aggregate(fns ...AggregateFunc) *TrasnscriptStructureSelect {
	tss.fns = append(tss.fns, fns...)
	return tss
}

// Scan applies the selector query and scans the result into the given value.
func (tss *TrasnscriptStructureSelect) Scan(ctx context.Context, v any) error {
	if err := tss.prepareQuery(ctx); err != nil {
		return err
	}
	tss.sql = tss.TrasnscriptStructureQuery.sqlQuery(ctx)
	return tss.sqlScan(ctx, v)
}

func (tss *TrasnscriptStructureSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(tss.fns))
	for _, fn := range tss.fns {
		aggregation = append(aggregation, fn(tss.sql))
	}
	switch n := len(*tss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		tss.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		tss.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := tss.sql.Query()
	if err := tss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
